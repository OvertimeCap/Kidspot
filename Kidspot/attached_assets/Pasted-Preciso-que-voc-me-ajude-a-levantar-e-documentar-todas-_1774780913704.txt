Preciso que você me ajude a levantar e documentar todas as informações do projeto KidSpot existente, para que possamos iniciar o desenvolvimento do Backoffice Web de forma totalmente integrada ao app atual, sem quebrar nenhuma funcionalidade existente.

Por favor, analise o repositório atual e gere os seguintes documentos e levantamentos:

1. Arquivo CLAUDE.md
Crie ou atualize o arquivo CLAUDE.md na raiz do projeto com:

Nome do projeto, stack tecnológica principal (linguagem, framework do app mobile, framework do backend)

Comandos essenciais do projeto (dev, build, test, lint)

Caminho dos diretórios principais (api, models, services, pipeline)

Convenções de código adotadas no projeto (nomenclatura, padrão de commits, formatação)

Lista de variáveis de ambiente necessárias, sem os valores reais

2. Documentação do Banco de Dados
Levante e documente:

Schema completo das tabelas existentes com nomes de colunas e tipos de dados

Relacionamentos entre tabelas (chaves primárias e estrangeiras)

Qual banco de dados está sendo utilizado (PostgreSQL, MySQL, SQLite, MongoDB)

Se não houver schema formal, gere um arquivo schema.prisma ou diagrama ER com base na estrutura atual

3. Mapa Completo das Rotas da API
Liste todos os endpoints existentes em formato de tabela Markdown contendo:

Método HTTP (GET, POST, PUT, DELETE)

Path completo (ex: /api/locais)

Descrição da funcionalidade

Exemplo de request e response em JSON

Nível de acesso (público ou autenticado)

Como a autenticação está implementada (JWT, session, OAuth)

4. Estrutura de Arquivos e Pastas
Gere um diagrama em formato de árvore (tree) da estrutura atual do projeto, destacando:

Onde ficam as rotas da API

Onde ficam os modelos do banco

Onde ficam as regras de negócio (services)

Onde fica o pipeline de ingestão da IA (se já existir)

5. Integrações Externas Ativas
Identifique e documente todos os serviços externos já conectados ao app:

API de IA em uso (ex: OpenAI) e como a chave é gerenciada

APIs de mapas (ex: Google Maps, Mapbox) e quais endpoints são chamados

Serviço de armazenamento de fotos (ex: AWS S3, Cloudinary, Firebase Storage)

Plataforma de deploy atual (ex: Railway, Render, Heroku, VPS)

6. Resumo de Compatibilidade
Ao final, gere um relatório curto respondendo:

Quais partes do sistema atual precisarão ser adaptadas para suportar o backoffice?

Existe algum conflito arquitetural entre o que já existe e o que será criado?

Qual é a recomendação de estrutura de pastas para adicionar o backoffice ao repositório sem impactar o app?

Entregue cada seção em um arquivo Markdown separado dentro de uma pasta /docs na raiz do projeto. Ao final, confirme quais informações não foram possíveis de levantar automaticamente e liste o que precisará ser preenchido manualmente.

