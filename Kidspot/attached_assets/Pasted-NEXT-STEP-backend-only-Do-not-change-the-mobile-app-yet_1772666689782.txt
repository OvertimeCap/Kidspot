NEXT STEP (backend only). Do not change the mobile app yet.

1) Implement backend API routes (prefix /api) inside `server/routes.ts` and any needed helper files.
Create these endpoints:

- GET /api/health -> { ok: true }
- GET /api/kidspot/ping-db -> { db: true } (only checks DB connection)
- POST /api/places/search
  Body: { city: "Franca" | "Ribeirão Preto", query?: string, lat?: number, lng?: number, radiusMeters?: number }
  Behavior:
   - If lat/lng provided: use Google Places Nearby Search (or Places Search) near that point.
   - Else: use Text Search biased to the city (Franca-SP or Ribeirão Preto-SP).
  Return only minimal fields: place_id, name, formatted_address, location(lat,lng), types, rating, user_ratings_total.

- GET /api/places/details?place_id=...
  Return only minimal fields: place_id, name, formatted_address, location, types, rating, user_ratings_total, opening_hours (if exists), photos (only photo references or URLs if allowed), website, formatted_phone_number.

2) Environment variables:
- GOOGLE_PLACES_API_KEY (required)
- DATABASE_URL (required)
Add/update `.env.example` with these keys.

3) Database schema (KidSpot layer):
Replace the current generic user schema with KidSpot tables using Drizzle (since it is already installed).
Create/update Drizzle schema files so that `db:push` creates:

- places_kidspot:
  place_id (PK, text)
  city (text)
  lat (numeric)
  lng (numeric)
  tags (jsonb nullable)
  created_at (timestamp default now)

- reviews:
  id (uuid PK default gen_random_uuid())
  place_id (FK -> places_kidspot.place_id)
  rating (int 1-5)
  kid_flags (jsonb)  // { trocador: boolean, cadeirao: boolean, banheiro_familia: boolean, espaco_kids: boolean, seguro: boolean }
  note (text nullable)
  created_at (timestamp default now)

- favorites:
  id (uuid PK default gen_random_uuid())
  user_key (text)  // simple anonymous user identifier for MVP (no auth yet)
  place_id (FK)
  created_at (timestamp default now)
  Unique (user_key, place_id)

4) Add KidSpot endpoints (backend only):
- POST /api/reviews  Body: { place_id, rating, kid_flags, note? }
- GET  /api/reviews?place_id=...
- POST /api/favorites/toggle Body: { user_key, place_id }
- GET  /api/favorites?user_key=...

5) Restrictions:
- Do NOT implement authentication yet.
- Do NOT implement the map yet.
- Do NOT refactor the mobile app.
- Keep code minimal and readable.

6) Deliverables / Acceptance:
- `GET /api/health` works.
- `GET /api/kidspot/ping-db` works when DATABASE_URL is set.
- `POST /api/places/search` returns results for Franca and Ribeirão Preto.
- `GET /api/places/details` works for a returned place_id.
- `db:push` succeeds and tables exist.
- Provide a short `docs/API.md` with curl examples for each endpoint.